name: cie-smart-prepaid-poc

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cie_smart_prepaid_poc
      POSTGRES_USER: poc_user
      POSTGRES_PASSWORD: poc_password
    ports:
      - "5432:5432"
    volumes:
      - pg-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U poc_user -d cie_smart_prepaid_poc"]
      interval: 5s
      timeout: 5s
      retries: 10

  mosquitto:
    image: eclipse-mosquitto:2
    ports:
      - "8883:8883"
    volumes:
      - ./infra/mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - ./infra/mosquitto/acl.conf:/mosquitto/config/acl.conf:ro
      - ./infra/mosquitto/certs:/mosquitto/certs:ro
      - mosquitto-data:/mosquitto/data

  backend:
    build:
      context: ./backend/poc-backend
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: cie_smart_prepaid_poc
      DB_USER: poc_user
      DB_PASSWORD: poc_password
      MQTT_BROKER_URL: ssl://mosquitto:8883
      MQTT_CA_CERT_PATH: /certs/ca.crt
      MQTT_CLIENT_CERT_PATH: /certs/backend-gateway.crt
      MQTT_CLIENT_KEY_PATH: /certs/backend-gateway.key
      MOCK_DONGLE_BASE_URL: http://mock-dongle:9001
      SERVER_PORT: 8080
    ports:
      - "8080:8080"
    volumes:
      - ./infra/mosquitto/certs:/certs:ro
    depends_on:
      postgres:
        condition: service_healthy
      mosquitto:
        condition: service_started

  mock-dongle:
    build:
      context: ./simulators/mock-dongle
    environment:
      DEVICE_ID: DONGLE-LAB-0001
      METER_ID: CIE-LAB-0001
      MQTT_HOST: mosquitto
      MQTT_PORT: "8883"
      MQTT_CA_CERT_PATH: /certs/ca.crt
      MQTT_CLIENT_CERT_PATH: /certs/DONGLE-LAB-0001.crt
      MQTT_CLIENT_KEY_PATH: /certs/DONGLE-LAB-0001.key
      BACKEND_HEARTBEAT_URL: http://backend:8080/api/v1/devices/DONGLE-LAB-0001/heartbeat
    ports:
      - "9001:9001"
    volumes:
      - ./infra/mosquitto/certs:/certs:ro
    depends_on:
      - mosquitto

  payment-simulator:
    build:
      context: ./simulators/payment-simulator
    environment:
      BACKEND_CALLBACK_URL: http://backend:8080/api/v1/payments/callback
      FORWARD_TO_BACKEND: "true"
    ports:
      - "9000:9000"
    depends_on:
      - backend

volumes:
  pg-data:
  mosquitto-data:
