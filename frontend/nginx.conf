server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA (React Router BrowserRouter) : toute route inconnue retombe sur index.html,
    # le routage cote client fait le reste.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
